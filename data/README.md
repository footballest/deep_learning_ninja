# Data directory

- `raw/` – original datasets
- `interim/` – intermediate processed data
- `processed/` – final data ready for modeling

This directory is gitignored by default; track data separately (or with DVC).
